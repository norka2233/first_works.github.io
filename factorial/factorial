var number = prompt("Enter the number");
var factorial = 1;

document.write("Factorial of the number " + number + "= ");

do {

	if (number == 0) {
		factorial = 1;
		break;
}

	factorial = factorial * number;
	number --;

}

	while (number > 0);
	document.write(factorial);
